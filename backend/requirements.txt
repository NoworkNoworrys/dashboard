# GeoIntel Backend — Python dependencies
# Install with: pip install -r requirements.txt

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
sse-starlette>=1.8.2
requests>=2.31.0
feedparser>=6.0.11

# Testing
pytest>=8.0.0
httpx>=0.27.0          # required by FastAPI TestClient for integration tests
